import {StyleSheet} from 'react-native';

const fullTextWrapper = {
  opacity: 0,
  position: 'absolute',
  left: 0,
  top: 0
}

const viewMoreText = {
  color: 'blue'
}

const transparent = {
  opacity: 0
}


export default StyleSheet.create({})
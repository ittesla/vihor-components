// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconWaypoints extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'waypoints'
  }
}

export default IconWaypoints

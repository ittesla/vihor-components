// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconFeather extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'feather2'
  }
}

export default IconFeather

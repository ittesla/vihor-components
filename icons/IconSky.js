// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconSky extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'sky'
  }
}

export default IconSky

// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconWatch extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'watch'
  }
}

export default IconWatch

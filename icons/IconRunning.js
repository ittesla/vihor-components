// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconRunning extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'road-running'
  }
}

export default IconRunning

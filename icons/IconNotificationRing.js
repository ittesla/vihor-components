// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconNotificationRing extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'group-27i'
  }
}

export default IconNotificationRing

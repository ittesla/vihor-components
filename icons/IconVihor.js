// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconVihor extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'vihor'
  }
}

export default IconVihor

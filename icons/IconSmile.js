// @flow
import React from 'react'
import Icon from './Icon'
import type {IconProps} from './Icon'

class IconSmile extends Icon {
  constructor(props: IconProps) {
    super(props)

    this.name = 'smile'
  }
}

export default IconSmile

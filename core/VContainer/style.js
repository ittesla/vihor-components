import {StyleSheet} from 'react-native'
import {colorSecondaryTxt} from '../../themes/default/colors'
import {SCREEN_HEIGHT} from 'vihor_components/viewHelper';

const container = {
  height: SCREEN_HEIGHT,
  backgroundColor:colorSecondaryTxt
}

export default StyleSheet.create({
  container
})